@model IEnumerable<SauWeb.Models.Appoinment>

@{
    ViewData["Title"] = "Index";
}

<h1>Randevu Alma </h1>

<p>
    <a asp-action="Create">Yeni Randevu Oluştur</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Doctor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Day)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Time)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Full)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Doctor.DoctorName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Day)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Time)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Full)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AppomentId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AppomentId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AppomentId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
